Put your Swagger UI screenshot here as `swagger-ui.png`.

To take it: run `npm start`, open http://localhost:3000/docs, expand an endpoint,
click "Try it out" → "Execute" so a live response is visible, then screenshot the
page (macOS: Cmd+Shift+4).
